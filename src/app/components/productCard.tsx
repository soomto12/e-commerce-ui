import React from "react"
import { productTypes } from "../../../types/types"


const ProductCartd : React.FC <productTypes> = ({id, name,shortDescription, sizes, colors, images, price,   })=>{

return (
<div>
    
</div>

)

}


export default ProductCartd